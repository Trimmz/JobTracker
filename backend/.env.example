# Server Configuration
PORT=5000
NODE_ENV=production

# CORS Configuration (set this to your frontend URL in production)
FRONTEND_URL=http://localhost:5173

# Database
# Option 1: PostgreSQL (recommended for production - free on Render)
# Set DATABASE_URL to use PostgreSQL (automatically provided by Render)
# DATABASE_URL=postgresql://user:password@host:port/database

# Option 2: SQLite (for local development)
# If DATABASE_URL is not set, SQLite will be used
# DB_DIR can specify where to store the SQLite file (optional)
# DB_DIR=./

# Admin Account (REQUIRED - set these to create your admin user)
# These credentials are NOT stored in the code - keep them secure!
ADMIN_USERNAME=your-admin-username
ADMIN_PASSWORD=your-secure-password
