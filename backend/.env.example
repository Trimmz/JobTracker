# Server Configuration
PORT=5000
NODE_ENV=production

# CORS Configuration (set this to your frontend URL in production)
FRONTEND_URL=http://localhost:5173

# Database
# For local development, this can be omitted (will use current directory)
# For production on Render, set this to your persistent disk mount path
DB_DIR=/var/data

# Admin Account (REQUIRED - set these to create your admin user)
# These credentials are NOT stored in the code - keep them secure!
ADMIN_USERNAME=your-admin-username
ADMIN_PASSWORD=your-secure-password
